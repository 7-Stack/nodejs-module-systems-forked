const { square } = require('../test')
// is divisible by two

exports.isDivisibleByTwo = (number) => {
  return number % 2 === 0;
}

exports.isDivisibleByThree = (number) => {
  return number % 3 === 0;
}